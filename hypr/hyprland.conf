
# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,highres,auto,1


ecosystem:no_update_news=true

###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = kitty
$fileManager = nemo
$browser = zen-browser

#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

exec-once = ~/.config/hypr/xdg-portal-hyprland
exec-once = nm-applet --indicator
#exec-once = waybar
exec-once = dms run
exec-once = ignis-init
#exec-once = dunst
exec-once = jamesdsp & zapzap --start-minimized
exec-once = vesktop --start-minimized
exec-once = org.telegram.desktop --start-minimized
#exec-once = swaync
exec-once = wal -R
#exec-once = waypaper --restore
#exec-once = ags
exec-once = clipse -listen     #for clipse-gui clipboard to run it on startup
exec-once = swww-daemon
exec-once = waypaper-engine daemon
#exec-once = /usr/bin/gammastep-indicator
exec-once = blueman-applet
exec-once = wl-clipboard-history -t
exec-once = dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = systemctl --user import-environment WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
exec-once = /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once = wl-paste --type text --watch cliphist store #Stores only text data
exec-once = wl-paste --type image --watch cliphist store #Stores only image data
exec-once = $HOME/.config/hypr/scripts/sleep.sh
exec-once = hyprpm enable hyprchroma
#exec-once = better-control --start-minimized
exec-once = hyprctl setcursor Bibata-Original-Ice 26

#############################
### ENVIRONMENT VARIABLES ###
#############################

xwayland {

  enabled = true
	
  force_zero_scaling = true
}

# See https://wiki.hyprland.org/Configuring/Environment-variables/


#####################
### LOOK AND FEEL ###
#####################

# Make every window float instead of tile by default
#windowrule = float

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 3
    gaps_out = 5

    border_size = 0
    
    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(33ccffee) rgba(00ff99ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 7

    # Change transparency of focused and unfocused windows
    active_opacity = 0.99
    inactive_opacity = 0.95

    shadow {
    	enabled = true
    	range = 4
    	render_power = 3
    	color = rgba(1a1a1aee)
    }
    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = true
        size = 10
        passes = 2
        new_optimizations = yes
        ignore_opacity = true
        noise = 0.10
        brightness = 0.90 

        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
#animations {
#    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

#    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

#    animation = windows, 1, 7, myBezier
#    animation = windowsOut, 1, 7, default, popin 80%
#    animation = border, 1, 10, default
#    animation = borderangle, 1, 8, default
#    animation = fade, 1, 7, default
#    animation = workspaces, 1, 6, default
#}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = true
    disable_splash_rendering = true
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =
    numlock_by_default = true
    follow_mouse = 1
    accel_profile = flat
    force_no_accel = true
    

    sensitivity = 0.3 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
        
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
#gestures {
#    workspace_swipe = false
#}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


###################
### KEYBINDINGS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, X, exec, $terminal
bind = $mainMod, Q, killactive
#bind = $mainMod SHIFT, M, exit
#bind = $mainMod, L, exec, swaylock
bind = $mainMod, E, exec, $fileManager
bind = $mainMod, space, togglefloating
bind = $mainMod,F,fullscreen
#bind = $mainMod, R, exec, wofi --show drun --allow-images
bind = $mainMod, R, exec, rofi -show drun
#bind = $mainMod, P, pseudo # dwindle
bind = $mainMod SHIFT, R, exec, gpu-screen-recorder-gtk
#bind = $mainMod, J, togglesplit # dwindle
bind = $mainMod, Z, exec , $browser
#bind = $mainMod, S, exec, grim -g "$(slurp)" - | swappy -f #take a scrennshot
#bind = $mainMod, D, exec ,wofi
#bind = $mainMod, V, exec, cliphist list | wofi --dmenu | cliphist decode | wl-copy  #launch cliphist clipboard manager
bind = $mainMod, V, exec , clipse-gui
#bind = $mainMod, F9, exec, wlsunset -t 5000   #for bluelight filter
bind = $mainMod, F9, exec, gammastep -O 4000   #for bluelight filter
#bind = $mainMod, F10, exec, pkill wlsunset
bind = $mainMod, F10, exec, pkill gammastep
bind = $mainMod SHIFT, B, exec, better-control
bind = Alt, Tab, movefocus, d
#bind = $mainMod, N, exec, swaync-client -t -sw              #swaync
bind = $mainMod SHIFT, R, exec, killall -SIGUSR2 waybar     # restart waybar
bind = SUPER SHIFT, W, exec, swww img "$(find ~/Wallpapers -type f | shuf -n 1)" --transition-fps 50 --transition-type grow --transition-pos 1,1
bind = ctrl shift , esc, exec, missioncenter
bind = $mainMod SHIFT, A, exec , waydroid show-full-ui
bind = SUPER, L, exec, dms ipc call lock lock
bind = SUPER SHIFT, M, exec,  dms ipc call powermenu toggle

# Keyboard backlight
bind = , $mainMod code:73, exec, brightnessctl -d *::kbd_backlight set +33%         # code:73 is F7
bind = , $mainMod code:73, exec, brightnessctl -d *::kbd_backlight set 33%-

# brightness and volume
bind = ,XF86AudioLowerVolume, exec, pactl -- set-sink-volume 0 -5%     # Fn + F<whatever>
bind = ,XF86AudioRaiseVolume, exec, pactl -- set-sink-volume 0 +5%     # Fn + F<whatever>
bind = ,XF86AudioMute, exec, pactl -- set-sink-mute 0 toggle            # Fn + F<whatever>
bind = ,XF86AudioMicMute, exec, pactl -- set-source-mute 0 toggle       # Fn + F<whatever>
bind = ,XF86MonBrightnessDown, exec, brightnessctl s 5%-                # Fn + F<whatever>
bind = ,XF86MonBrightnessUp, exec, brightnessctl s +5%                  # Fn + F<whatever>

# Screenshot a window
bind = $mainMod, PRINT, exec, hyprshot -z -m window
#bind = $maindMod SHIFT, S, exec, hyprshot -m region 
# Screenshot a monitor
bind = , PRINT, exec, hyprshot -z -m output
# Screenshot a region
bind = $mainMod SHIFT, PRINT, exec, hyprshot -z -m region

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Example special workspace (scratchpad)
bind = $mainMod, S, togglespecialworkspace, magic
bind = $mainMod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow

# Laptop multimedia keys for volume and LCD brightness
bindel = ,XF86AudioRaiseVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindel = ,XF86AudioLowerVolume, exec, wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindel = ,XF86AudioMute, exec, wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindel = ,XF86AudioMicMute, exec, wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindel = ,XF86MonBrightnessUp, exec, brightnessctl s 10%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl s 10%-

# Requires playerctl
bindl = , XF86AudioNext, exec, playerctl next
bindl = , XF86AudioPause, exec, playerctl play-pause
bindl = , XF86AudioPlay, exec, playerctl play-pause
bindl = , XF86AudioPrev, exec, playerctl previous

##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$
#windowrulev2 = opacity 0.9 0.7, class:^(zen)$ # zen browser
# Example windowrule v21
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
windowrulev2 = suppressevent maximize, class:.* # You'll probably like this

# float everything by default
#windowrulev2 = float, class:.*
windowrulev2 = float, class: nwg-look
windowrulev2 = float, class: clipse-gui
windowrulev2 = float, class: blueman-manager
windowrulev2 = float, class: org.pulseaudio.pavucontrol
windowrulev2 = size 650,600,class:^(org.pulseaudio.pavucontrol)$
windowrulev2 = float, class: org.kde.kdeconnect.handler
windowrulev2 = float, class: org.kde.kdeconnect.daemon
windowrulev2 = float, class: garuda
windowrulev2 = float, class: nvidia-settings
windowrulev2 = float, class: jamesdsp
windowrulev2 = float, class: thunar
windowrulev2 = float, class: peazip
windowrulev2 = float, class: wihotspot
windowrulev2 = float, class: stacer
windowrulev2 = float, class: com.github.tenderowl.frog
windowrulev2 = float, class: nemo
windowrulev2 = float, class: dolphin
windowrulev2 = size 800,600,class:^(nemo)$
windowrulev2 = float, class: io.github.ilya_zlobintsev.LACT
windowrulev2 = float, class: steam
windowrulev2 = float, class: snapper-toolis
windowrulev2 = float, class: setup-assistant
windowrulev2 = float, class: garuda-network-assistant
windowrulev2 = float, class: garuda-boot-options
windowrulev2 = float, class: garuda-assistant
windowrulev2 = float, class: garuda-rani
windowrulev2 = float, class: com.jaoushingan.WaydroidHelper
windowrulev2 = float, class: ark
windowrulev2 = float, class: nm-connection-editor
windowrulev2 = float, class: org.gnome.firmware
windowrulev2 = float, class: org.gnome.FileRoller
windowrulev2 = float, class: me.kavishdevar.librepods
windowrulev2 = float, class: xdg-desktop-portal-gtk
windowrulev2 = float, class: wineboot.exe
windowrulev2 = float, class: control.exe
windowrulev2 = float, class: org.kde.kget
windowrulev2 = float, class: protonvpn-app
#windowrulev2 = float, class: steam
windowrulev2 = float, class: org.kde.haruna
windowrulev2 = float, class: org.vysp3r.ProtonPlus


#these rules means when a download pop up open it'll open up in the center and floating
windowrulev2 = float, class:^(xdg-desktop-portal-gtk)(.)$
windowrulev2 = size 45%, class:^(xdg-desktop-portal-gtk)(.)$
windowrulev2 = move center, class:^(xdg-desktop-portal-gtk)(.)$


# XWayland Video Bridge
#windowrule = opacity 0.0 override, class:^(xwaylandvideobridge)$
#windowrule = noanim, class:^(xwaylandvideobridge)$
#windowrule = noinitialfocus, class:^(xwaylandvideobridge)$
#windowrule = maxsize 1 1, class:^(xwaylandvideobridge)$
#windowrule = noblur, class:^(xwaylandvideobridge)$
#windowrule = nofocus, class:^(xwaylandvideobridge)$

# for hyprshot
windowrulev2 = float, title:^(.*Hyprshot.*)$

# Custom Scripts
source = ~/.config/hypr/ENVariables.conf
source = ~/.config/hypr/animations.conf


# battery drain
#experimental:integer_scaling = true
misc:vfr = true
decoration:blur:enabled = false
decoration:shadow:enabled = false

# for blur
#layerrule = blur, waybar
#layerrule = ignorezero, waybar
#layerrule = ignorealpha 0.5, waybar
layerrule = blur,background
layerrule = blur,overlay
layerrule = blur,top
layerrule = blur,fullscreen   


################
### PLUGINS ###
################


# hyprcursor
#env = HYPRCURSOR_THEME,Breeze_Light
#env = HYPRCURSOR_SIZE,24

env = HYPRCURSOR_THEME,Bibata-Original-Ice
env = HYPRCURSOR_SIZE,26


# hyprchroma
windowrulev2 = plugin:chromakey,fullscreen:0
#chromakey_background = 7,8,17


#dynamic-cursor
plugin:dynamic-cursors {

    # enables the plugin
    enabled = true

    # for mode = stretch
    stretch {

        # controls how much the cursor is stretched
        # this value controls at which speed (px/s) the full stretch is reached
        limit = 3000

        # relationship between speed and stretch amount, supports these values:
        # linear             - a linear function is used
        # quadratic          - a quadratic function is used
        # negative_quadratic - negative version of the quadratic one, feels more aggressive
        function = quadratic
    }

    # configure shake to find
    # magnifies the cursor if its is being shaken
    shake {

        # enables shake to find
        enabled = true

        # use nearest-neighbour (pixelated) scaling when shaking
        # may look weird when effects are enabled
        nearest = true

        # controls how soon a shake is detected
        # lower values mean sooner
        threshold = 6.0

        # magnification level immediately after shake start
        base = 4.0
        # magnification increase per second when continuing to shake
        speed = 4.0
        # how much the speed is influenced by the current shake intensitiy
        influence = 0.0

        # maximal magnification the cursor can reach
        # values below 1 disable the limit (e.g. 0)
        limit = 0.0

        # time in millseconds the cursor will stay magnified after a shake has ended
        timeout = 2000

        # show cursor behaviour `tilt`, `rotate`, etc. while shaking
        effects = false

        # enable ipc events for shake
        # see the `ipc` section below
        ipc = false
    }

    # use hyprcursor to get a higher resolution texture when the cursor is magnified
    # see the `hyprcursor` section below
    hyprcursor {

        # use nearest-neighbour (pixelated) scaling when magnifing beyond texture size
        # this will also have effect without hyprcursor support being enabled
        # 0 / false - never use pixelated scaling
        # 1 / true  - use pixelated when no highres image
        # 2         - always use pixleated scaling
        nearest = 1

        # enable dedicated hyprcursor support
        enabled = true

        # resolution in pixels to load the magnified shapes at
        # be warned that loading a very high-resolution image will take a long time and might impact memory consumption
        # -1 means we use [normal cursor size] * [shake:base option]
        resolution = -1

        # shape to use when clientside cursors are being magnified
        # see the shape-name property of shape rules for possible names
        # specifying clientside will use the actual shape, but will be pixelated
        fallback = clientside
    }

}

cursor {
    enable_hyprcursor = true
    hide_on_key_press = true
    sync_gsettings_theme = false
}

# Hyprfocus


